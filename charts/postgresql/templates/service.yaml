apiVersion: v1
kind: Service
metadata:
  name: postgresql
  labels:
    app.kubernetes.io/name: postgresql
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: postgresql
  ports:
    - name: postgresql
      port: {{ .Values.service.port }}
      targetPort: 5432
