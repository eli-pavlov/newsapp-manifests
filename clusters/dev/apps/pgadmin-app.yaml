apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: newsapp-pgadmin-dev
  namespace: argocd
  annotations:
    argocd.argoproj.io/depends-on: '["sealed-secrets-controller"]'
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: newsapp-dev
  ignoreDifferences:
  - group: apps
    kind: Deployment
    name: pgadmin-dev-pgadmin4
    namespace: development
    jsonPointers:
    - /spec/template/spec/containers/0/readinessProbe/path
    - /spec/template/spec/containers/0/readinessProbe/port
    - /spec/template/spec/containers/0/readinessProbe/scheme
    - /spec/template/spec/containers/0/startupProbe/path
    - /spec/template/spec/containers/0/startupProbe/port
    - /spec/template/spec/containers/0/startupProbe/scheme
    - /spec/template/spec/containers/0/volumeMounts/0/subPath
    - /spec/template/spec/containers/0/readinessProbe/enabled
  source:
    repoURL: https://helm.runix.net
    chart: pgadmin4
    targetRevision: 1.50.0
    helm:
      releaseName: pgadmin-dev
      values: |
        replicaCount: 2
        image:
          repository: dpage/pgadmin4
          tag: "9.8"
          pullPolicy: IfNotPresent
        service:
          type: ClusterIP
          port: 80
          # targetPort usually 80 inside the container
        ingress:
          enabled: true
          ingressClassName: nginx
          annotations:
            kubernetes.io/ingress.class: "nginx"
            # optional: limit access by IP range
            # nginx.ingress.kubernetes.io/whitelist-source-range: "x.x.x.x/32,y.y.y.y/32"
          hosts:
            - host: pgadmin-dev.weblightenment.com
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - secretName: pgadmin-dev-tls
              hosts:
                - pgadmin-dev.weblightenment.com
        resources:
          requests:
            cpu: 100m
            memory: 512Mi
          limits:
            cpu: 200m
            memory: 512Mi
        startupProbe:
          enabled: true
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 20
        readinessProbe:
          enabled: true
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 6
        # -- END CORRECTION --
        existingSecret: pgadmin-auth
  destination:
    server: https://kubernetes.default.svc
    namespace: development
  ignoreDifference:
  - group: apps
    kind: Deployment
    jsonPointers:
        - /spec/template/spec/containers/0/startupProbe/httpGet
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true